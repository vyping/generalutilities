<?xml version="1.0" encoding="UTF-8"?>

<selector

    xmlns:android="http://schemas.android.com/apk/res/android">

    <item
        android:state_pressed="true">
        <shape
            android:shape="rectangle">
            <stroke
                android:color="@color/colorNegro"
                android:width="3dp" />
            <solid
                android:color="@color/GreyLight050" />
            <corners
                android:radius="24dp" />
        </shape>
    </item>

    <item>
        <shape
            android:shape="rectangle">
            <stroke
                android:color="@color/colorNegro"
                android:width="3dp" />
            <solid
                android:color="@color/colorTransparente" />
            <corners
                android:radius="24dp" />
        </shape>
    </item>

</selector>
